import java.util.*;

public class find {
    public static Scanner scn = new Scanner(System.in);
    private static int returnIndex(int[] arr, int data) {
        int foundAtIndex = -1;
        for(int ele : arr){
            if(arr[i] == data)
                foundAtIndex = i;
                break;
        }
        return foundAtIndex;
           
    }
    return foundAtIndex;

    private static static main(String[] args) {
        
    }
    
}
